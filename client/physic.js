'use strict';

let cadavreCluster = [];

function applyPhysic(obj){
    if(!obj) {
        return;
    }

}

function initPhysicObject(x, y, size, vector) {
    return {
        x: x,
        y: y,
        size: size,
        vector: vector,
    }
}

function addCadavretoCluster(cadavre) {
	
}